// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/tanapon395/playlist-video/ent/giveaway"
	"github.com/tanapon395/playlist-video/ent/predicate"
	"github.com/tanapon395/playlist-video/ent/promotion"
)

// GiveawayUpdate is the builder for updating Giveaway entities.
type GiveawayUpdate struct {
	config
	hooks      []Hook
	mutation   *GiveawayMutation
	predicates []predicate.Giveaway
}

// Where adds a new predicate for the builder.
func (gu *GiveawayUpdate) Where(ps ...predicate.Giveaway) *GiveawayUpdate {
	gu.predicates = append(gu.predicates, ps...)
	return gu
}

// SetGiveawayName sets the giveawayName field.
func (gu *GiveawayUpdate) SetGiveawayName(i int) *GiveawayUpdate {
	gu.mutation.ResetGiveawayName()
	gu.mutation.SetGiveawayName(i)
	return gu
}

// AddGiveawayName adds i to giveawayName.
func (gu *GiveawayUpdate) AddGiveawayName(i int) *GiveawayUpdate {
	gu.mutation.AddGiveawayName(i)
	return gu
}

// AddForgiveawayIDs adds the forgiveaway edge to Promotion by ids.
func (gu *GiveawayUpdate) AddForgiveawayIDs(ids ...int) *GiveawayUpdate {
	gu.mutation.AddForgiveawayIDs(ids...)
	return gu
}

// AddForgiveaway adds the forgiveaway edges to Promotion.
func (gu *GiveawayUpdate) AddForgiveaway(p ...*Promotion) *GiveawayUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return gu.AddForgiveawayIDs(ids...)
}

// Mutation returns the GiveawayMutation object of the builder.
func (gu *GiveawayUpdate) Mutation() *GiveawayMutation {
	return gu.mutation
}

// RemoveForgiveawayIDs removes the forgiveaway edge to Promotion by ids.
func (gu *GiveawayUpdate) RemoveForgiveawayIDs(ids ...int) *GiveawayUpdate {
	gu.mutation.RemoveForgiveawayIDs(ids...)
	return gu
}

// RemoveForgiveaway removes forgiveaway edges to Promotion.
func (gu *GiveawayUpdate) RemoveForgiveaway(p ...*Promotion) *GiveawayUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return gu.RemoveForgiveawayIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (gu *GiveawayUpdate) Save(ctx context.Context) (int, error) {

	var (
		err      error
		affected int
	)
	if len(gu.hooks) == 0 {
		affected, err = gu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*GiveawayMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			gu.mutation = mutation
			affected, err = gu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(gu.hooks) - 1; i >= 0; i-- {
			mut = gu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, gu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (gu *GiveawayUpdate) SaveX(ctx context.Context) int {
	affected, err := gu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (gu *GiveawayUpdate) Exec(ctx context.Context) error {
	_, err := gu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gu *GiveawayUpdate) ExecX(ctx context.Context) {
	if err := gu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (gu *GiveawayUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   giveaway.Table,
			Columns: giveaway.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: giveaway.FieldID,
			},
		},
	}
	if ps := gu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := gu.mutation.GiveawayName(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: giveaway.FieldGiveawayName,
		})
	}
	if value, ok := gu.mutation.AddedGiveawayName(); ok {
		_spec.Fields.Add = append(_spec.Fields.Add, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: giveaway.FieldGiveawayName,
		})
	}
	if nodes := gu.mutation.RemovedForgiveawayIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   giveaway.ForgiveawayTable,
			Columns: []string{giveaway.ForgiveawayColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: promotion.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := gu.mutation.ForgiveawayIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   giveaway.ForgiveawayTable,
			Columns: []string{giveaway.ForgiveawayColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: promotion.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, gu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{giveaway.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// GiveawayUpdateOne is the builder for updating a single Giveaway entity.
type GiveawayUpdateOne struct {
	config
	hooks    []Hook
	mutation *GiveawayMutation
}

// SetGiveawayName sets the giveawayName field.
func (guo *GiveawayUpdateOne) SetGiveawayName(i int) *GiveawayUpdateOne {
	guo.mutation.ResetGiveawayName()
	guo.mutation.SetGiveawayName(i)
	return guo
}

// AddGiveawayName adds i to giveawayName.
func (guo *GiveawayUpdateOne) AddGiveawayName(i int) *GiveawayUpdateOne {
	guo.mutation.AddGiveawayName(i)
	return guo
}

// AddForgiveawayIDs adds the forgiveaway edge to Promotion by ids.
func (guo *GiveawayUpdateOne) AddForgiveawayIDs(ids ...int) *GiveawayUpdateOne {
	guo.mutation.AddForgiveawayIDs(ids...)
	return guo
}

// AddForgiveaway adds the forgiveaway edges to Promotion.
func (guo *GiveawayUpdateOne) AddForgiveaway(p ...*Promotion) *GiveawayUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return guo.AddForgiveawayIDs(ids...)
}

// Mutation returns the GiveawayMutation object of the builder.
func (guo *GiveawayUpdateOne) Mutation() *GiveawayMutation {
	return guo.mutation
}

// RemoveForgiveawayIDs removes the forgiveaway edge to Promotion by ids.
func (guo *GiveawayUpdateOne) RemoveForgiveawayIDs(ids ...int) *GiveawayUpdateOne {
	guo.mutation.RemoveForgiveawayIDs(ids...)
	return guo
}

// RemoveForgiveaway removes forgiveaway edges to Promotion.
func (guo *GiveawayUpdateOne) RemoveForgiveaway(p ...*Promotion) *GiveawayUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return guo.RemoveForgiveawayIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (guo *GiveawayUpdateOne) Save(ctx context.Context) (*Giveaway, error) {

	var (
		err  error
		node *Giveaway
	)
	if len(guo.hooks) == 0 {
		node, err = guo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*GiveawayMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			guo.mutation = mutation
			node, err = guo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(guo.hooks) - 1; i >= 0; i-- {
			mut = guo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, guo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (guo *GiveawayUpdateOne) SaveX(ctx context.Context) *Giveaway {
	gi, err := guo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return gi
}

// Exec executes the query on the entity.
func (guo *GiveawayUpdateOne) Exec(ctx context.Context) error {
	_, err := guo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (guo *GiveawayUpdateOne) ExecX(ctx context.Context) {
	if err := guo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (guo *GiveawayUpdateOne) sqlSave(ctx context.Context) (gi *Giveaway, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   giveaway.Table,
			Columns: giveaway.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: giveaway.FieldID,
			},
		},
	}
	id, ok := guo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Giveaway.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := guo.mutation.GiveawayName(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: giveaway.FieldGiveawayName,
		})
	}
	if value, ok := guo.mutation.AddedGiveawayName(); ok {
		_spec.Fields.Add = append(_spec.Fields.Add, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: giveaway.FieldGiveawayName,
		})
	}
	if nodes := guo.mutation.RemovedForgiveawayIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   giveaway.ForgiveawayTable,
			Columns: []string{giveaway.ForgiveawayColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: promotion.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := guo.mutation.ForgiveawayIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   giveaway.ForgiveawayTable,
			Columns: []string{giveaway.ForgiveawayColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: promotion.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	gi = &Giveaway{config: guo.config}
	_spec.Assign = gi.assignValues
	_spec.ScanValues = gi.scanValues()
	if err = sqlgraph.UpdateNode(ctx, guo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{giveaway.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return gi, nil
}
