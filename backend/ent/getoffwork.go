// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team13/app/ent/getoffwork"
)

// GetOffWork is the model entity for the GetOffWork schema.
type GetOffWork struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// GetOffWork holds the value of the "GetOffWork" field.
	GetOffWork time.Time `json:"GetOffWork,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GetOffWorkQuery when eager-loading is set.
	Edges GetOffWorkEdges `json:"edges"`
}

// GetOffWorkEdges holds the relations/edges for other nodes in the graph.
type GetOffWorkEdges struct {
	// Whenendwork holds the value of the whenendwork edge.
	Whenendwork []*EmployeeWorkingHours
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WhenendworkOrErr returns the Whenendwork value or an error if the edge
// was not loaded in eager-loading.
func (e GetOffWorkEdges) WhenendworkOrErr() ([]*EmployeeWorkingHours, error) {
	if e.loadedTypes[0] {
		return e.Whenendwork, nil
	}
	return nil, &NotLoadedError{edge: "whenendwork"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GetOffWork) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{}, // id
		&sql.NullTime{},  // GetOffWork
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GetOffWork fields.
func (gow *GetOffWork) assignValues(values ...interface{}) error {
	if m, n := len(values), len(getoffwork.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	gow.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field GetOffWork", values[0])
	} else if value.Valid {
		gow.GetOffWork = value.Time
	}
	return nil
}

// QueryWhenendwork queries the whenendwork edge of the GetOffWork.
func (gow *GetOffWork) QueryWhenendwork() *EmployeeWorkingHoursQuery {
	return (&GetOffWorkClient{config: gow.config}).QueryWhenendwork(gow)
}

// Update returns a builder for updating this GetOffWork.
// Note that, you need to call GetOffWork.Unwrap() before calling this method, if this GetOffWork
// was returned from a transaction, and the transaction was committed or rolled back.
func (gow *GetOffWork) Update() *GetOffWorkUpdateOne {
	return (&GetOffWorkClient{config: gow.config}).UpdateOne(gow)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (gow *GetOffWork) Unwrap() *GetOffWork {
	tx, ok := gow.config.driver.(*txDriver)
	if !ok {
		panic("ent: GetOffWork is not a transactional entity")
	}
	gow.config.driver = tx.drv
	return gow
}

// String implements the fmt.Stringer.
func (gow *GetOffWork) String() string {
	var builder strings.Builder
	builder.WriteString("GetOffWork(")
	builder.WriteString(fmt.Sprintf("id=%v", gow.ID))
	builder.WriteString(", GetOffWork=")
	builder.WriteString(gow.GetOffWork.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GetOffWorks is a parsable slice of GetOffWork.
type GetOffWorks []*GetOffWork

func (gow GetOffWorks) config(cfg config) {
	for _i := range gow {
		gow[_i].config = cfg
	}
}
