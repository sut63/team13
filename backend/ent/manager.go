// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/team13/app/ent/manager"
)

// Manager is the model entity for the Manager schema.
type Manager struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Email holds the value of the "email" field.
	Email string `json:"email,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ManagerQuery when eager-loading is set.
	Edges ManagerEdges `json:"edges"`
}

// ManagerEdges holds the relations/edges for other nodes in the graph.
type ManagerEdges struct {
	// Managers holds the value of the managers edge.
	Managers []*Orderproduct
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ManagersOrErr returns the Managers value or an error if the edge
// was not loaded in eager-loading.
func (e ManagerEdges) ManagersOrErr() ([]*Orderproduct, error) {
	if e.loadedTypes[0] {
		return e.Managers, nil
	}
	return nil, &NotLoadedError{edge: "managers"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Manager) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // name
		&sql.NullString{}, // email
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Manager fields.
func (m *Manager) assignValues(values ...interface{}) error {
	if m, n := len(values), len(manager.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	m.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[0])
	} else if value.Valid {
		m.Name = value.String
	}
	if value, ok := values[1].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field email", values[1])
	} else if value.Valid {
		m.Email = value.String
	}
	return nil
}

// QueryManagers queries the managers edge of the Manager.
func (m *Manager) QueryManagers() *OrderproductQuery {
	return (&ManagerClient{config: m.config}).QueryManagers(m)
}

// Update returns a builder for updating this Manager.
// Note that, you need to call Manager.Unwrap() before calling this method, if this Manager
// was returned from a transaction, and the transaction was committed or rolled back.
func (m *Manager) Update() *ManagerUpdateOne {
	return (&ManagerClient{config: m.config}).UpdateOne(m)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (m *Manager) Unwrap() *Manager {
	tx, ok := m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Manager is not a transactional entity")
	}
	m.config.driver = tx.drv
	return m
}

// String implements the fmt.Stringer.
func (m *Manager) String() string {
	var builder strings.Builder
	builder.WriteString("Manager(")
	builder.WriteString(fmt.Sprintf("id=%v", m.ID))
	builder.WriteString(", name=")
	builder.WriteString(m.Name)
	builder.WriteString(", email=")
	builder.WriteString(m.Email)
	builder.WriteByte(')')
	return builder.String()
}

// Managers is a parsable slice of Manager.
type Managers []*Manager

func (m Managers) config(cfg config) {
	for _i := range m {
		m[_i].config = cfg
	}
}
